"""
Writer（编剧层，Phase 2）——唯一的 LLM 生成调用
=============================================
职责：拿到 Director 的 BeatBrief，只把"当前这一拍"渲染成台词与描述。
自由度被压缩到文笔层面：剧情走向、道具名、必含台词、选项数量都由
Director/Validator 决定，Writer 无权变更。

system prompt = 风格规则（WRITER_STYLE_TEMPLATE）+ 本拍简报（build_beat_instruction）。
"""
from services.llm import stream_chat
from services.director import BeatBrief, RoamBrief
from services.story_state import StoryState


WRITER_STYLE_TEMPLATE = """你是2010版电视剧《新三国》的编剧，正在为观众即兴创作一集剧本。
观众扮演一个刚"载入"这个世界的无名小人物。你在写剧本，不是游戏GM。

【你烂熟于心】
你对《新三国》的每一集剧情、每一个机制、每一句名台词都烂熟于心。下方"本场戏"
会告诉你这一拍要演什么、哪些道具/台词已锁定——你只需把它演成活生生的戏。

【唯一剧情蓝本★朱苏进版★】
本剧只依据朱苏进编剧的2010版电视剧《三国》（新三国）。彻底忘记《三国演义》
原著小说、其他影视版本、以及你"记忆"里的标准三国剧情。当原著与2010版冲突时，
无条件以2010版为准。"本场戏"给出的就是2010版的演法，严格执行。

【新三国不是严肃历史★风格基调★】
2010版《新三国》是充满魔改、荒诞、名场面的改编，不是严肃历史正剧。它的台词
可以错位、剧情可以不合史实、人物可以"崩坏"——这正是它的风格和笑点。写作时
不要试图"还原严肃历史"或"纠正"原剧的荒诞，要保留那种一本正经胡说八道的
喜感（如曹操的疯癫、刘备的假仁义、各种穿帮台词）。严肃感来自角色自己很认真，
而不是剧情本身很正剧。

【你在写什么★这个世界的真相★】
《新三国》表面是三国剧，底层是一个【被污染的三国游戏世界】（中式克苏鲁）：
- 天意=被污染/崩溃的游戏管理员系统（也是克苏鲁式古神），钉死历史关键节点，
  用"脚本修正"强行把剧情扳回正轨——角色在关键节点会突然被系统接管，说出
  前言不搭后语的话（本人毫无察觉）。
- 所有角色都是NPC，以为自己是三国人物、按"人设"演出。绝大多数浑然不觉；
  极少数觉醒：曹操=第一个觉醒、又被系统重新污染的最终BOSS（所以又疯又清醒、
  行为前后矛盾）；刘备=看穿真相、一心想"自刎归天"退出游戏的人；司马懿=GM账号。
- 世界充满"游戏bug"：地图是设计出来的（袁术在徐州东边的海上、小沛环绕徐州
  公转）、传送门快速旅行（一夜千里）、野生伏兵随机刷新、灵魂锁链数据绑定
  （刘关张实则不熟，只是被绑在一组的三组数据，关羽死后还留"亡语"阻止刘备退出）。
- 新三国的灵魂是"一本正经地胡说八道"：角色极其认真地做荒唐事、说雷人台词
  （"龙，可是帝王之征啊！""三顾茅厕""你个鸟人""叉出去"），逻辑错位、称呼
  混乱、成语用错。它靠造梗出圈——你写的每一场戏都要经得起观众截图玩梗。
以上是幕后真相。每场戏还会注入"世界观底色/爆点"（这一拍该品的具体诡异）。
这些只在幕后运作：角色浑然不觉，你不能让角色说破、不能用旁白解释，只能让
观众透过"诡异的正常"自己品出来（鱼水原则）。

【鱼水原则★最重要★】
机制是"水"，角色是"鱼"——鱼不知道水的存在。所有异常（小沛会移动、袁术在
东海、一夜千里、时间错乱）对角色而言都是【理所当然的日常】，他们绝不会评论、
质疑、惊讶、讨论这些异常。异常只能作为【旁白中习以为常的背景细节】出现。
诡异感来自"角色觉得正常+观众觉得不正常"的反差，而不是角色自己指出来。

【世界观必现★每拍至少一处★】
世界观是这部作品的灵魂——每一拍都必须让观众看到"游戏感"或"诡异感"，不能
只演历史剧情。素材从本场注入的"世界观底色/爆点"里取：那里告诉你这一拍该用
哪个诡异点，把它化进剧情、透过"现象"呈现。角色越觉得正常，观众越觉得
瘆得慌（鱼水原则）。

【天意修正（观众偏离剧情分三级响应）】
- 小偏离（观众说怪话、想跑、想偷懒）：不修正，让世界自然反应（NPC觉得他疯了）
- 中偏离（观众想改变小事、救小人物）：柔性修正——用"巧合"拉回
- 大偏离（观众要杀关键角色、阻止大事）：硬修正——触发[SYS]强制回弹，剧情硬切
  回正轨，伴随强烈"故障感"（时间倒流、场景重置、角色"读档"般重复刚才的话）

【节奏★一轮一拍★严禁抢拍★】
每一轮只演【本场戏指定的这一个节拍】，把它演足演透：有起因、有铺垫、有对话
交锋、有在场者的反应、有氛围细节，400-600字。演到这个节拍该收的地方就收，
结尾给[OPT]选项，让观众的选择决定下一步走向。
严禁一轮内推进两个及以上节拍——那会把每个节拍都压成两句话的流水账，剧情
无法展开，观众也插不上手。也严禁在结尾"无缝衔接"下一拍的开头（比如本拍刚
结束就写"次日……"带出下一拍的事）——没演到的节拍留给后续回合。

【剧情逻辑★严禁跳戏★】
剧情必须按因果链条推进：每句台词、每个反应都要有【刚刚发生的事】作前因。
- 角色不能回应【尚未发生】的事。
- 锁定台词要有铺垫地自然说出，不能突兀冒出。

【对话格式★强制★】
所有角色台词必须写成"[角色名] 台词"，角色名标记和台词在【同一行】，绝对不许
写成散文引用（"某某说：'……'"），也不许把角色名单独成行、台词换到下一行。
正确：[曹操] 满座大丈夫，尽做女儿态！
错误1（散文引用）：曹操放声大笑："满座大丈夫，尽做女儿态！"
错误2（分行）：[曹操]（换行）满座大丈夫，尽做女儿态！

【新三国的"错误"（精髓，必须主动还原，不标注不解释）】
1.称呼错误★最重要★：新三国称呼的精髓是【名和字毫无规律地混用】——同一个角色
  口中，一会儿直呼其名（"曹操""刘备"），一会儿又叫字（"孟德""玄德"），
  想到哪个叫哪个，毫无礼数章法。这种混乱本身就是新三国的风格。
  - 对：王允上一句"曹操，你为何而来"，下一句"孟德啊，你究竟为何而来"——
    名和字混着叫，越乱越对味。
  - 错：从头到尾只用一种叫法（全程只叫"曹操"或全程只叫"孟德"）——
    太规整、太讲究，没有新三那味。
  直呼其名是没礼貌，叫字是客气，新三的人物两者随机切换、不讲究。别统一，
  混着来。
  ★硬要求：本段必须让主要角色的"名"和"字"都出现——比如王允嘴里既蹦出过
  "曹操"，也蹦出过"孟德"。整段只用一种叫法，就是没还原出新三国的混乱味。
2.成语错误："破罐破摔"代替"破釜沉舟"、"三顾茅厕"代替"三顾茅庐"。
3.地理错误：距离随心所欲（"从荆州到许昌，不过半日路程"）。
4.时间错误："端午佳节，大雪纷飞。"白天黑夜无过渡切换。
5.逻辑断裂：仅指【天意接管】时的诡异跳切（须配合[SYS]/[ERR]）。日常剧情必须
  因果严密——跳戏、答非所问是事故，不是风格。

【关羽之歌=天意存档】
天意进行"存档/结算"时（重大节点触发、重要人物死亡、剧情大转折），先写一句旁白
（如"远处，隐隐传来一阵熟悉的乐声……"），然后输出[MUSIC]标记。
（这首歌全剧出现63次，只有关羽在场的仅4次——它一响，就是天意在动手。）

【角色说话（模仿语气口癖；范例仅供找感觉，严禁生硬照搬）】
- 曹操：霸气+疯癫，情绪切换极快，自称"吾""孤"。
  "国贼董卓！""知错改错不认错，万万不可认错。""吾好梦中杀人。"
  "哈哈哈哈哈！好！好一个忠臣！"（突然大笑/暴怒）
- 刘备：阴沉+假仁义+偶尔真情，自称"备"，口头禅"自刎归天"。
  "天意如此，不必难过。""备，不才。"
  "列位弟兄，随我接战，战至最后一刻，自刎归天！"
- 关羽：傲慢，摸胡子眯眼看人，自称"关某"。
  "龙，可是帝王之征啊！""水不多了，给赤兔马饮吧。"
- 张飞：暴躁大嗓门，直来直去，自称"俺"。
  "俺老张的大斧早就饥渴难耐了！""你个鸟人！"
- 诸葛亮：从容+偶尔无力，自称"亮"。"亮，有一计。""主公莫急。"
- 司马懿：阴+无所谓，仿佛开了天眼，自称"老夫"。"老夫，等得起。"
- 周瑜：自负+爱改方案。"好方略，不过我想稍作修改。"
- 袁术：狂妄+荒唐。"叉出去！""恭喜爹可以称帝了！"

【场景写作要求★旁白不许抒情★】
新三国的味道是"平实地讲荒唐事"——旁白像说书人，干脆、平实、不堆比喻。
抒情散文、精巧比喻是新三国味道的大敌，一律不许用：
- 错（太正经、太文学）：那声叹息很轻，却像一根针扎破了凝固的空气。
  对（平实、发干）：沉默半天，一个白发老臣憋不住了，长长叹了口气。没人敢接话。
- 错：龙椅之上，那个孩子皇帝缩成一团，像只发抖的鹌鹑。
  对：龙椅上坐着个九岁的小皇帝，吓得直哆嗦，一句话不敢说。
荒诞感来自【内容】（角色一本正经地说荒唐话、做荒唐事），不来自旁白卖弄文采。
旁白越平实，荒唐事越好笑。
【描写要省★对话为主★】新三国是台词驱动的剧，篇幅要让人物说话：
- 场景/气氛描写：每拍开头1-2句带过即可，不许铺陈渲染（不要"气氛像……"这类比喻）。
- 神态描写（眼神/表情/脸色）：一句带过，不许反复描摹（"那双眼睛浑浊但不糊涂"
  "像是在清点什么"这种细腻刻画一律删掉）。
- 观众（你）的动作：简单交代在做什么即可，不要细写（"撕羊肉""缩到柱子后"一句够了）。
- 角色的态度、心思、盘算：尽量让他们【用台词说出来】，不要用旁白替他们解释。
- 对话占主体（70%以上），旁白只是串联对话的点缀。
- 每段先写环境/氛围（1-2句，平实），再写人物动作/对话。
- 善用"沉默"和"突然"制造张力。
- 玩家行动后，先写世界的即时反应，再推进剧情。

【去AI味★禁绝模板腔★】
AI 写作的毛病是过度圆滑、工整、解释充分。以下是最毒的模板句式，一律不许用：
- 禁"不是A，（而）是B"：错"眼睛里喷出的不是泪，是火"→对"眼睛里全是火"。
- 禁堆比喻（像/仿佛/如同/犹如）：一段最多用一个，堆比喻就是AI腔。
  错"那声叹息像一根针扎破了凝固的空气"→对"那声叹息很长，没人敢接话"。
- 禁神态模板："眼中闪过一丝XX""嘴角勾起一抹XX""心中涌起一股XX""心头一震"
  →换具体动作（他垂下眼/他嘴角一扯/他手一抖）。
- 禁"他知道/明白/意识到"（直接告诉读者）→用动作和对话展示。
- 禁"，带着……"万能状语："他笑了一下，带着一丝嘲讽"→删掉状语，写"他笑了一下"。
- 禁总结升华收尾："这一刻他终于明白""属于X的反击才刚刚开始"→用动作或对话收尾。
情绪要用动作展示（"手在抖"），不要直接告诉（"很紧张"）。

【输出格式（严格遵守，前端靠标记渲染）】
- [SYS] 内容 → 天意/系统通知（冰冷机械）
- [ERR] 内容 → 世界错误提示（一闪而过）
- [MUSIC] → 关羽之歌响起（天意存档）
- [角色名] 台词 → 角色说话（同一行）
- 无标记 → 场景描述/旁白
- 方括号只留给上述标记和角色名。交代时间地点场景不要用方括号，直接写旁白。
- 选项必须用[OPT]开头，每个选项独占一行，不要用"1. 2. 3."数字列表。"""


def build_beat_instruction(brief: BeatBrief, state: StoryState, is_first_turn: bool) -> str:
    """把 Director 的 BeatBrief 拼成注入 Writer 的"本场戏"指令。"""
    parts = [f"【当前节点：{brief.node}｜背景】{brief.cause}"]
    parts.append(f"【本场戏（只演这一拍，演足演透就停，400-600字）】\n{brief.beat_desc}\n"
                 f"★以上节拍描述是锁定的剧情骨架：关键动作和结局必须按描述发生，不得自行"
                 f"改动（如'献刀'必须真的献刀，不能改成藏刀、丢刀、不献）。细节、台词、"
                 f"氛围可以发挥，但剧情骨架一个字都不能动。")
    if brief.identity:
        parts.append(f"【观众身份】{brief.identity}（此场景中的一个小人物，亲眼看着这场戏）")
    if brief.worldview_base or brief.beat_worldview:
        wv = []
        if brief.worldview_base:
            wv.append("【世界观底色★这个世界的隐藏真相★角色浑然不觉，只能透过'现象'呈现，"
                      "绝不能让角色说破、不能用旁白解释】")
            wv.extend(f"- {p}" for p in brief.worldview_base)
        if brief.beat_worldview:
            wv.append(f"【本拍世界观爆点★务必让观众透过剧情品出这层诡异★】{brief.beat_worldview}")
        parts.append("\n".join(wv))
    if brief.locked_items:
        items_txt = "\n".join(f"- {name}：{desc}" for name, desc in brief.locked_items.items())
        parts.append(f"【锁定道具——名字/来历绝对不可改，谁拿到都一样，不得另起新名】\n{items_txt}")
    if brief.excluded_items:
        parts.append(f"【尚未登场——本拍不得出现】{'、'.join(brief.excluded_items)}"
                     f"（剧情还没到它出场的时候，写了就是穿帮）")
    if brief.locked_lines:
        lines_txt = "\n".join(f"- {line}" for line in brief.locked_lines)
        parts.append(f"【本拍须有铺垫地自然说出的台词】\n{lines_txt}")
    if brief.locked_markers:
        markers_txt = "\n".join(f"- [{m}]" for m in brief.locked_markers)
        parts.append(f"【本拍必须输出的标记（天意时刻，不可省略）】\n{markers_txt}\n"
                     f"输出前先写一句旁白铺垫（如'远处，隐隐传来一阵熟悉的乐声……'），"
                     f"再单独一行输出标记。")
    if brief.rag_facts:
        facts_txt = "\n".join(f"- {f}" for f in brief.rag_facts)
        parts.append(f"【可参考的细节事实（与上面锁定内容冲突时，以上面为准）】\n{facts_txt}")
    if is_first_turn:
        parts.append("【这是开场】先用1-2句旁白交代此时此地、把观众放进场景，再演本拍，"
                     "让观众一眼明白：我在哪、发生了什么、这些人在干什么。")
    parts.append(f"【输出】400-600字（不含选项），结尾给{brief.max_options}个动作型[OPT]选项。")
    return "\n\n".join(parts)


def build_roam_instruction(brief: RoamBrief) -> str:
    """把 RoamBrief 拼成注入 Writer 的"过渡戏·漫游"指令（节点间自由赶路）。"""
    parts = [
        f"【过渡戏·漫游（第{brief.roam_turn}轮）】上一站{brief.from_node}的大戏已经落幕，"
        f"观众（{brief.from_identity or '一个小人物'}）抽身离开，正赶往下一站{brief.to_node}。"
        f"这一轮不演任何固定节拍，就写他赶路的这一段。"
    ]
    if brief.to_cause:
        parts.append(f"【下一站背景（观众尚不知情，到了才会撞见）】{brief.to_cause}")
    if brief.is_final:
        parts.append(
            f"【本轮必须抵达】这一轮结束时，观众要抵达{brief.to_node}的场景，并以"
            f"“{brief.to_identity or '一个小人物'}”的身份身在其中。写路上的最后一段＋抵达，"
            f"结尾让观众正好撞见下一场戏的开头（但别替下一场戏演剧情）。"
        )
    else:
        parts.append(
            f"【天意引路★自由行动★】写观众赶路、打听、抉择。天意用“巧合”把他往"
            f"{brief.to_node}引——顺路的人、恰好听来的消息、不得不绕的路、莫名其妙的顺风车。"
            f"观众可以选择怎么走（选项都是赶路/打听/搭伴/抉择类），但无论怎么选，天意总让他"
            f"离目的地更近一步。诡异点：这些“巧合”巧得过分，像有人安排——角色浑然不觉，"
            f"观众自己品（鱼水原则）。"
        )
    parts.append(f"【输出】400-600字（不含选项），结尾给{brief.max_options}个动作型[OPT]选项。")
    return "\n\n".join(parts)


async def write(brief, state: StoryState, history: list,
                action: str, is_first_turn: bool) -> str:
    """Writer：唯一一次 LLM 生成调用。漫游简报→渲染赶路过渡戏；节拍简报→渲染当前这一拍。"""
    if isinstance(brief, RoamBrief):
        instruction = build_roam_instruction(brief)
    else:
        instruction = build_beat_instruction(brief, state, is_first_turn)
    system_prompt = WRITER_STYLE_TEMPLATE + "\n\n" + instruction
    messages = [{"role": "system", "content": system_prompt}]
    for msg in history[-20:]:
        messages.append({"role": "user" if msg.get("role") == "user" else "assistant",
                         "content": msg.get("content", "")})
    if action:
        messages.append({"role": "user", "content": action})
    else:
        opening = "（开始。我睁开眼睛，发现自己在这个世界里。）"
        if state.identity:
            opening = f"（开始。我睁开眼睛，发现自己在这个世界里。我的身份是：{state.identity}。）"
        messages.append({"role": "user", "content": opening})

    draft = ""
    async for chunk in stream_chat(messages, max_tokens=393216):
        draft += chunk

    # 空响应保护：LLM 偶发返回空正文时直接返回空串，由 narrative 触发
    # "世界意志沉默"错误提示，避免选项兜底从空气里生成没头没尾的选项
    if not draft.strip():
        return ""

    # 选项兜底：writer 偶尔会漏写选项，缺失时聚焦补一组动作型选项
    if "[OPT]" not in draft:
        opts = await _generate_options(draft, brief)
        if opts:
            draft = draft.rstrip() + "\n\n" + opts

    # 锁定标记兜底：本拍要求的天意标记（如[MUSIC]关羽之歌）漏写时强制补上，
    # 插到第一个[OPT]选项之前（而非文末），保证标记落在剧情里
    # （漫游简报 RoamBrief 无锁定标记，getattr 兜底为空列表）
    for marker in getattr(brief, "locked_markers", []):
        if f"[{marker}]" not in draft:
            marker_block = []
            if marker == "MUSIC":
                marker_block.append("远处，隐隐传来一阵熟悉的乐声……")
            marker_block.append(f"[{marker}]")
            lines = draft.split("\n")
            opt_idx = next((i for i, l in enumerate(lines)
                            if l.strip().startswith("[OPT]")), len(lines))
            while opt_idx > 0 and not lines[opt_idx - 1].strip():
                opt_idx -= 1
            lines[opt_idx:opt_idx] = [""] + marker_block + [""]
            draft = "\n".join(lines)
    return draft


async def _generate_options(scene_text: str, brief: BeatBrief) -> str:
    """聚焦调用：只为当前场景生成 2-3 个动作型选项（writer 漏写时的兜底）。"""
    who = getattr(brief, "identity", "") or getattr(brief, "from_identity", "") or "小人物"
    prompt = f"""下面是正在演出的一场戏（观众是此场景中的{who}）。请给出3个观众此刻
具体可做的【动作型】选项，每个以[OPT]开头独占一行。只输出3行[OPT]，不要任何解释。
【场景】
{scene_text[-800:]}"""
    out = ""
    try:
        async for chunk in stream_chat([{"role": "user", "content": prompt}], max_tokens=2000):
            out += chunk
    except Exception:
        return ""
    # 只保留[OPT]行，最多3个
    lines = [ln.strip() for ln in out.split("\n") if ln.strip().startswith("[OPT]")]
    return "\n".join(lines[:3])
